更多详细操作可访问https://www.gtkapp.com/formsdesigner/

窗体设计管理方法，以GTKWinFormsApp工程为例：

【推荐】使用.net原生窗体应用工程来管理窗体，在原生工程下设计窗体，无任何限制，可同时使用。

1、在GTKWinFormsApp工程目录下，通过复制GTKWinFormsApp.csproj工程文件创建，重命名为GTKWinFormsApp_Designer.csproj

2、移除引用类“GTKSystem.Windows.Forms”

3、恢复Winform原生工程配置，把框架“输出类型”改成“Windows应用程序”，“windows窗体”勾选“为该项目启动windows窗体”。

4、添加新建配置文件Directory.Build.props ，配置如下：

<Project>
	<PropertyGroup>
		<BaseIntermediateOutputPath>obj\$(MSBuildProjectName)</BaseIntermediateOutputPath>
		<IntermediateOutputPath>$(BaseIntermediateOutputPath)\$(TargetFramework)</IntermediateOutputPath>
	</PropertyGroup>
</Project>

如有异常提示，只需删除obj文件夹，让vs自动重新生成即可。
